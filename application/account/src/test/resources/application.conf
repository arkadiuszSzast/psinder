mail {
    activateAccount {
       templateId = "activate-account-template-id"
       subject = "Activate Your Account"
       sender = "activate-account-sender@mail.com"
    }
}

jwt {
    activateAccount {
        secret = "activate_jwt_test_secret"
        issuer = "psinder_test"
        expirationInMillis = "4320000"
    }
}