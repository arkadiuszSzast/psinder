jwt {
    domain = "https://jwt-test-domain/"
    audience = "jwt-test-audience"
    realm = "jwt-test-realm"
    issuer = "jwt-test-issuer"
    secret = "test-secret"
}
database {
    name = "test-database-name"
    connectionString = "test-database-password"
}
eventStore {
    connectionString = "event-store-connection-string"
}
datadog {
    apiKey = "dd-api-key"
    applicationKey = "dd-application-key"
    hostTag = "dd-host-tag"
}
application {
    env = "dev"
}
tracing {
    enabled = "false"
}
sentry {
    enabled = "false"
    dsn = "sentry-dsn"
}
